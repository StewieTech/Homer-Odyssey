# Adapters

Adapters render portable package contracts into target-specific layouts. The Codex adapter adds the generated marker and source provenance to Markdown, JSON, and other text surfaces without mutating the canonical package.
