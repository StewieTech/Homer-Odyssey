# Transforms

Transforms will compose classification, generalization, redaction, capability filtering, dependency resolution, and overlay application. The read-only plan reports the intended projection without applying transforms.
