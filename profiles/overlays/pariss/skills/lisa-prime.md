Use Pariss GitHub issues, recent PRs, durable registers, and QA docs for dedupe. Promote to `agent-ready`, `ralph-prime`, or `Lorie/HITL` only after the Evidence Lock is cleared and the issue-factory gate passes.

Chrome session evidence is allowed only after scripted browser evidence cannot prove the real path and the required session, log, and screenshot packet can be captured safely.
