Use `.agents/goals/<goal-id>/`, Pariss label compatibility, issue-factory-core, and the Pariss execution adapter. The base branch is `developSIT`, and same-run execution is allowed only when the user explicitly selects EXECUTE mode and supplies authorization.

Use the canonical Pariss native execution-profile router; do not encode a second model map. Keep PRD/report issues out of Ralph execution and cap ordinary issue candidates and execution lists according to the adapter.
