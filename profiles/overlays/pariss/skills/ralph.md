Fetch the current Pariss remote base and branch from `origin/developSIT`, preserve or isolate unrelated local changes, implement one accepted GitHub issue, run target validation and applicable Docker worker evidence, and publish one PR with freshness proof and `Closes #<issue>`.

Use the GitHub connector in web runs and the existing authenticated local toolchain in local runs. Do not bootstrap new credential-bearing tools, merge, or deploy.
