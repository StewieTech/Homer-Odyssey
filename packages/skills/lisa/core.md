# Evidence-running QA

Run the smallest target-owned scenario and capture observations before evaluation. Keep actor evidence separate from evaluator judgment, avoid sensitive user content, and route only reproducible findings.

Use the [evidence contract](./helpers/contract.md). An incomplete run returns a blocker or no-op result.
